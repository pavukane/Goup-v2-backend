# Goup-v2-backend
Backend side of Goup, a social media built with React for frontend + Flask for backend

Run command: flask run
Pull changes to local: git pull

TODO: add AWS RDS for cloud MySQL so we dont have to deal with local settings for MySQL server
